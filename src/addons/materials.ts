import { Logger } from '../utils/logger.js';

export const materialsPage = () => {
    const logger = Logger.createContext('addons::materials::materialsPage');

    logger.info('Materials page loaded!');
};
