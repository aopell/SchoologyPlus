import { Logger } from '../utils/logger';

export const materialsPage = () => {
    const logger = Logger.createContext('pages::materials::materialsPage');

    logger.info('Materials page loaded!');
};
