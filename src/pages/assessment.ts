import { Logger } from '../utils/logger';

export const assessmentPage = () => {
    const logger = Logger.createContext('pages::assessment::assessmentPage');

    logger.info('Assessment page loaded!');
};
